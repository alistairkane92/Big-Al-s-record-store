

<div id="inventory_title">
    <p> Artist </p>
    <p> Album </p>
    <p> Stock Level </p>
    <p> Delete </p>
</div>


<% @artists.each do |artist| %>
    <div class = "record">
        <%= artist.name() %>
        <div class = "albums">
        <p><% artist.albums.each do |album| %></p>
        <p><%= album.name %></p>
        <p><%= album.stock_level %></p>
        </div>
        <% end %>
        <p><form action="/recordstore/artists/delete/<%=artist.id%>" method="POST">
            <input type="submit" value="Delete" id = "btn"></p>
        </form>
    </div>
<% end %>
